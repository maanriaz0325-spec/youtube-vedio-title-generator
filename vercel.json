{
  "version": 2,
  "builds": [
    { "src": "api/generate-titles.js", "use": "@vercel/node" },
    { "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "dist" } }
  ],
  "routes": [
    { "src": "/api/generate-titles", "dest": "/api/generate-titles.js" },
    { "src": "/api/(.*)", "dest": "server.ts" },
    { "src": "/(.*\\.(js|css|ico|png|svg|json))", "dest": "/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}